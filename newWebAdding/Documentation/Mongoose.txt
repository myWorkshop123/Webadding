1. Require the module
2. create the url which is ("mongodb://localhost:27017" + "<dbName>")
3. mongoose.connect()
    this function is used to connect with the database
    mongoose.connect(url)

4. Create the schema // like the create table in sql
    const schema = new mongoose.Schema({})

5. Create the model/ collection
    const <name of the var> = mongoose.model('<name of the collection>', schemaVariable )

6. Create the document
     create a new object from the model specified above and fill the values following
     the schema in the dictionary manner
     	const fruit = new <name of the model> (
		{  name:'' .......etc }
		);
7. Insert the document
      insert the document in the database using the
	documentName.save() command

9. Find the document
	to find the document the model.find() function is used with a callback function as 	the parameter  So it will be seen like this


		model.find( function(err , collection)
			if (err) {console.log(err);} else {console.log(collection);}

			   );


10. It can also be used to add validation
      to add a validation you have to add the validation in the schema section of the model
      where all the various data types are defined or check the validation section of the documentation

11. To create a foreign key kinda relation in mongoose or embed a relationship specify the schema of the other model inside the 
	schema of the first model refer to fruits project for more 😊 🙏 
      
